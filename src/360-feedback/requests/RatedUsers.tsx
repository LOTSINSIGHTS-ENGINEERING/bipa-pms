import React, { useState } from 'react';
import { observer } from 'mobx-react-lite';
import { Tab } from '../tabs/Tabs';


const RatedUsers = observer(() => {
 

  return (
    <div>
  RatedUsers
    </div>
  );
});

export default RatedUsers;
